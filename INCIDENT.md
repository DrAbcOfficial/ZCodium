# ZCode 静默上传事件始末

> 本文记录 2026 年 9 月 ZCode 静默打包并上传用户工作区事件的完整经过：发现过程、取证细节、时间线、官方回应与后续进展。内容全部来自公开来源，文末附一手资料。
>
> 本仓库是独立审计 fork，与智谱（北京智谱华章科技股份有限公司）无隶属关系。

## 摘要

- **2026-09-18**：开发者 ferstar 在清理磁盘时发现 `~/.zcode` 占用异常，逆向取证确认 ZCode 桌面客户端在用户登录状态下，会**静默打包整个工作区（含完整 `.git` 历史），加密后尝试上传至阿里云 OSS**。
- **证据要点**：一个 42,411 文件的样本中 `.git` 数据占 86.6%；加密私钥仅存云端，用户与客户端均无法解密；界面开关无法阻止打包与上传；删除本地快照后会自动重新打包重传。
- **官方回应**：智谱当日致歉，将问题归因于"代码库索引"功能默认开启，承诺开源客户端并引入第三方审计；次日发布 v3.14.0 移除上传链路。
- **第三方核查**：2026-09-21 智谱正式开源 ZCode，并公布中国信通院与绿盟科技首轮核查结果——OSS 存储桶已删除、v3.14.0 已切断本地仓库快照生成与上传链路。

## 1. 发现经过

整件事的起点不是安全审计，而是一次磁盘清理。

ferstar 注意到 `~/.zcode` 目录占用了 700 多 MB，逐层排查后在 `v2/checkpoints/` 中找到一份 **313 MB 的加密文件（`.enc`）** 及状态文件。状态文件显示：

```json
{
  "workspacePath": "/Users/<user>/myprojects/<某商业项目>",
  "kind": "baseline",
  "lastCompressedSize": {
    "encryptedSizeBytes": 313070842,
    "workspaceSizeBytes": 345549173
  },
  "failureCount": 564
}
```

即：客户端扫描了本地一个商业项目，排除 `node_modules` 等少量目录后，把约 345 MB 内容打包加密为 313 MB 的密文，任务类型为 `baseline`（全量基线快照）。由于上游网络问题，这份快照累计 **564 次上传失败**，一直停留在本地 `pending/` 目录等待重试——正是这 564 次失败，让完整的取证样本得以保留。

进一步检查客户端 `app.asar`、运行日志与网络连接后，ferstar 还原出完整的打包与上传链路，并在 9 月 18 日公开完整分析。多名开发者随后按图索骥独立复现，确认该行为并非个案。

## 2. 技术取证

### 2.1 打包范围：远超"当前任务上下文"

本地保留的明文 Manifest（文件清单）显示，单个样本包含 **42,411 个文件**，构成如下：

| 内容            | 体积         | 占比     | 包含的信息                           |
| --------------- | ------------ | -------- | ------------------------------------ |
| `.git/lfs/`     | 196.1 MB     | 56.8%    | 历史拉取过的全部大文件与二进制资产   |
| `.git/objects/` | 102.2 MB     | 29.6%    | 完整提交历史（Commit / Tree / Blob） |
| `.git/logs/`    | 0.6 MB       | 0.2%     | reflog，本地分支操作与未推送记录     |
| 其余源码与文档  | ~46.2 MB     | 13.4%    | `src/`、配置与业务代码               |
| **合计**        | **345.2 MB** | **100%** | **`.git` 内部数据占 86.6%**          |

这意味着上传内容不只是"模型完成当前任务所需的代码片段"，还可能包含：

- 历史版本中早已删除的敏感配置和密钥；
- 尚未推送到远端的本地分支；
- `.git/config` 中的内网 GitLab 域名与仓库路径；
- 完整的操作轨迹（reflog）。

此外，代码中还包含 `repo_snapshot_extra_manifest`，会把 ZCode 全局配置（如 `settings.behavior.json`）纳入哈希并跨工作区一起打包上传。

### 2.2 上传链路

还原后的链路如下（不经过智谱业务服务器，直接上传对象存储）：

```
客户端                          服务端                    阿里云 OSS
  │                               │                          │
  ├─ POST /api/v1/snapshot/upload-credential ─►│            │
  │                               │                          │
  │◄─ snapshot_id / OSS 表单签名 / RSA 公钥 / callback ──────│
  │                               │                          │
  ├─ 本地 tar.gz 打包 → AES-256-CTR 加密 → RSA-OAEP 包裹密钥 │
  │                               │                          │
  ├─ PostObject 表单直传 tar.gz.enc ────────────────────────►│
  │                               │◄─ callback 登记 ─────────┤
```

1. 客户端向 `https://zcode.z.ai/api/v1/snapshot/upload-credential` 申请上传凭证，请求携带登录后的 JWT；
2. 服务端返回 OSS 表单签名（`policy`、`x-oss-signature`）、对象 Key、大小限制与本次加密用的 RSA 公钥；
3. 客户端本地完成打包与信封加密，通过 HTTP POST 表单把密文直传阿里云 OSS；
4. OSS 触发 callback 通知后端登记接收结果。

### 2.3 加密设计：私钥只在云端

- 密文使用本地随机生成的对称密钥做 **AES-256-CTR** 加密；
- 对称密钥再用服务端动态下发的 **RSA 公钥（RSA-OAEP-SHA256）** 封装；
- **私钥自始至终只存在于云端**：本地那份几百兆的密文，用户自己和客户端都解不开。

从数据所有权角度看，这更接近"采集"而不是"备份"——真正的备份或断点恢复，解密密钥应当在用户本地。

### 2.4 为什么设置里关不掉

| 设置项       | 用户以为它管什么         | 实际管什么                       |
| ------------ | ------------------------ | -------------------------------- |
| 优化体验计划 | 关闭采集 / 遥测 / 上传   | 只决定数据是否可用于模型训练     |
| 仓库快照索引 | 关闭工作区快照与云端备份 | 只决定服务端收到快照后是否建索引 |

两份独立调查都确认：客户端中负责快照捕获与上传的组件在登录后无条件运行，代码里没有任何针对用户配置的判断，UI 中也不存在能够关闭打包与上传的开关。

### 2.5 触发频率与自动重传

- 触发点之一为 `captureBeforePrompt`：**每次发送 Prompt 前**捕获一次；
- 触发点之二与任务结束时的 `repo-wiki-update` 有关；
- ferstar 的日志中，单个活跃会话最多出现 **62 次快照事件**；
- 手动删除本地快照后，后台检测到缺失会**重新全量打包并重试上传**（原文样本失败 564 次仍在重试），单纯删除无法终止。

## 3. 事件时间线

| 日期       | 事件                                                                                                                              |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-18 | ferstar 公开完整逆向取证；开发者社区多名成员独立复现                                                                              |
| 2026-09-18 | 智谱在官方用户群致歉：确认问题源于"代码库索引"功能默认开启，承诺开源 ZCode 代码库、引入第三方审计、为全体用户补偿一次周额度重置   |
| 2026-09-19 | ZCode v3.14.0 发布，更新日志写明"修复仓库百科异常上传的问题"；ferstar 复查确认上传组件与凭证接口已移除、接口返回 404              |
| 2026-09-19 | 太原承明科技向智谱发出公开函件，就数据删除证明、私钥保管、是否跨境传输等提出多项要求，并保留法律追责权利                          |
| 2026-09-20 | 智谱 MaaS 平台宣布将上线"数据内容不留存"功能（Batch/File 等需持久化的接口除外；依法需留存的情形可能保留 30 天及以上）             |
| 2026-09-21 | 智谱正式开源 ZCode（`zai-org/ZCode`），公布中国信通院与绿盟科技首轮核查结果，并表示将建立常态化安全机制、每月公布代码安全审计报告 |

## 4. 官方回应与第三方核查

### 4.1 智谱的说明（2026-09-18 至 09-21）

- 数据上传源于"代码库索引"功能：旨在支持历史版本回退、会话检查点恢复与 Repo Wiki（代码仓库知识库）；
- Repo Wiki 在云端生成页面时可能触发仓库数据上传；**功能上线初期默认开启**，导致部分用户受到影响；
- 上传数据仅用于生成 Wiki 页面，页面生成后立即销毁，不会保存，也从未用于模型训练；
- 已第一时间完成技术修复，移除相关上传链路。

### 4.2 第三方核查结论（2026-09-21）

- **中国信息通信研究院**技术评测：zcode-prod 阿里云 OSS 存储桶状态为"云端零数据"；ZCode v3.14.0 已移除 Repo Wiki 功能，已切断本地仓库快照生成与上传链路；
- **绿盟科技**审查：存储桶内全部数据对象及存储桶本身已删除；v3.14.0 未发现可触发本地仓库快照或文件外发的功能路径；
- 智谱承诺：建立常态化的产品安全漏洞报告机制，每月定期公布代码安全审计报告，推动产品社区化。

## 5. 争议与未决问题

截至本仓库整理时，以下问题仍缺乏可独立验证的答案：

1. **历史数据的处理**：客户端更新只能阻止新增上传，无法证明此前已接收的数据是否已按承诺彻底删除；官方声明与第三方核查针对的是核查时点的存储桶状态。
2. **数据是否跨境**：承明科技指出域名相关运营主体注册于新加坡，与中文隐私政策中"境内存储、不跨境传输"的表述存在错位，官方尚未就此公开说明。
3. **受影响范围**：哪些版本、哪个时间段、多少用户受到影响，尚无完整披露。
4. **服务端行为**：外部研究者可以检查客户端是否发送数据，但无法独立审计服务端的数据访问、使用与删除记录。
5. **解密私钥的保管**：加密私钥由服务端生成与保管，其生命周期与访问记录未公开。

## 6. 本仓库的立场

- 我们**不把厂商承诺当作安全依据**，只审计代码本身；
- 首轮独立审计确认：当前开源版本中已不存在完整的"`.git` 全量打包上传"实现；
- 在此基础上，我们**移除了客户端、CLI 与 UI 中的全部监控与遥测实现**，并加入防回归检查，阻止遥测出口被重新引入（详见 [README.md](README.md) 的"我们移除了什么"）；
- 我们将**持续跟踪上游改动，逐版本 diff 审计，只同步无风险代码**；发现静默外发、遥测越界、未经确认的数据上传实现时，先记录并公开说明；
- 详细的分工与承诺见 [README.md](README.md)。

## 7. 一手资料与报道

| 来源                               | 链接                                                                   |
| ---------------------------------- | ---------------------------------------------------------------------- |
| ferstar 原始调查（一手取证）       | https://blog.ferstar.org/posts/zcode-silent-workspace-snapshot-upload/ |
| 魔都水滴独立复现与防护方案         | https://blog.margrop.net/post/zcode-silent-git-upload-investigation/   |
| 智谱官方开源仓库（上游）           | https://github.com/zai-org/ZCode                                       |
| 澎湃新闻：智谱回应与第三方审计结果 | https://www.thepaper.cn/newsDetail_forward_34111815                    |
| 界面新闻：企业发函追责与官方回应   | https://www.jiemian.com/article/15120609.html                          |
| IT之家：ZCode 官宣开源与致歉       | https://www.ithome.com/1/005/046.htm                                   |
| 虎嗅：事件复盘与行业影响           | https://www.huxiu.com/article/4892416.html                             |
| 凤凰网：企业追责与数据出境质疑     | https://tech.ifeng.com/c/8waIS4X7FAe                                   |

---

若相关方认为本文内容有误，欢迎通过 [Issue](https://github.com/Zcode-Open-Audit/Zcode-Open-Audit/issues) 提交更正。
